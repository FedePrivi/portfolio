const ScrollAnimation = ({text}) => {

    return (
        <div className="scroll">

            <div className="animation"> <p>{text}</p> </div>

        </div>

    )
}

export default ScrollAnimation;

